import javax.swing.*;
import java.awt.*;

public class imIn extends JFrame {

    imIn(){
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
        setSize(250, 250);
        setLayout(new GridBagLayout());



        getContentPane();
        setVisible(true);
    }
}
